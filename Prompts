Patch the deployment so the DU BFF App Service receives these environment variables from Azure DevOps variable group/app settings:

BFF_OIDC_ISSUER
BFF_OIDC_CLIENT_ID
BFF_OIDC_CLIENT_SECRET
BFF_OIDC_REDIRECT_URI
BFF_OIDC_SCOPES
BFF_OIDC_AUDIENCE
BFF_OIDC_POST_LOGIN_REDIRECT
SESSION_MAX_AGE

Add the required Bicep params and add them into the duApiAppSettings properties block in infra/templates/web-app.bicep. Also update the pipeline parameter mapping if deploy.steps.yml passes variable group values into the Bicep deployment. Do not hardcode environment-specific values or secrets.


I need to complete OneAccount/OIDC integration for the DU BFF app.

Context:
- The BFF code already reads OIDC values from environment variables in aiae-du-bff config.py using:
  BFF_OIDC_ISSUER
  BFF_OIDC_CLIENT_ID
  BFF_OIDC_CLIENT_SECRET
  BFF_OIDC_REDIRECT_URI
  BFF_OIDC_SCOPES
  BFF_OIDC_AUDIENCE
  BFF_OIDC_POST_LOGIN_REDIRECT
  SESSION_MAX_AGE

Goal:
- Pass these variables from Azure DevOps variable group into the DU BFF App Service application settings during deployment.
- Do not hardcode real values, secrets, URLs, or environment-specific data in the repo.
- Use pipeline/variable group values only.

Please do end-to-end repo changes:
1. Find where DU App/BFF App Service app settings are defined in Bicep.
2. Add Bicep parameters for the above BFF OIDC variables if missing.
3. Add those variables into the App Service app settings block for the BFF/DU API app.
4. Find the Azure DevOps deployment YAML where Bicep parameters are passed.
5. Map the parameters from existing Azure DevOps variable group variables.
6. Show me all changed files and explain exactly what variable names I need to add in the Azure DevOps variable group.
7. Do not modify frontend login code unless it is actually required.




Update the repo changes for OneAccount OIDC PKCE integration.

We are using Authorization Code with PKCE, so client secret is NOT required.

Please update only:
1. infra/templates/web-app.bicep
2. pipelines/deploy.steps.yml

Add/wire these BFF runtime app settings:
BFF_OIDC_ISSUER
BFF_OIDC_CLIENT_ID
BFF_OIDC_REDIRECT_URI
BFF_OIDC_SCOPES
BFF_OIDC_AUDIENCE
BFF_OIDC_POST_LOGIN_REDIRECT
SESSION_MAX_AGE

Remove any newly added BFF_OIDC_CLIENT_SECRET parameter, pipeline mapping, or App Service app setting because PKCE does not need client secret.

Do not touch existing BFF_CLIENT_ID, BFF_CLIENT_SECRET, API_OIDC_BOOTSTRAP_* values, or frontend login code.




We found the root cause of the One Account login issue.

Current problem:
The frontend is Vite-based and the login URL is using `VITE_OIDC_CLIENT_ID` from:
src/du_api_webapp/aiae-du-frontend/aiae-du-portal/.env.example

Currently it has:
VITE_OIDC_CLIENT_ID=du-client

Because Vite values are baked into the frontend bundle at build time, the browser is sending `client_id=du-client` to One Account, causing:
400 - Invalid client_id

Required fix:
Do NOT change `API_OIDC_BOOTSTRAP_CLIENT_ID` or `BFF_CLIENT_ID`. Those are internal/bootstrap or BFF/API client values.

Only fix the frontend OIDC config path.

Please make the deployment variable-group-driven, not hardcoded.

Changes needed:
1. Update `.env.example` so frontend OIDC values are placeholders, not static values:
   - VITE_OIDC_AUTHORITY=<VITE_OIDC_AUTHORITY>
   - VITE_OIDC_CLIENT_ID=<VITE_OIDC_CLIENT_ID>
   - VITE_HOST_URL=<VITE_HOST_URL>
   - keep VITE_API_BASE_URL=<API_BASE_URL> as existing pattern

2. Update `pipelines/deploy.steps.yml` or the actual frontend build step to replace these placeholders before the frontend build:
   - <VITE_OIDC_AUTHORITY> should come from the environment/variable group value for One Account issuer, likely `bffOidcIssuer` or `BFF_OIDC_ISSUER`
   - <VITE_OIDC_CLIENT_ID> should come from the One Account registered client ID, likely `bffOidcClientId` or `BFF_OIDC_CLIENT_ID`
   - <VITE_HOST_URL> should come from the frontend/app base URL if required by the frontend callback logic

3. Ensure replacement happens before `npm build` / frontend build command, because Vite reads `VITE_*` variables at build time.

4. Do not hardcode Soumya’s client ID directly in `.env.example`, except if needed temporarily for local testing. Final solution must use Azure DevOps variable group / pipeline variables.

5. After the fix, the browser authorization URL should show:
   client_id=<One Account client id shared by Soumya>
   not:
   client_id=du-client

Please inspect the existing deploy script pattern where `API_BASE_URL` is replaced and extend the same pattern for `VITE_OIDC_CLIENT_ID`, `VITE_OIDC_AUTHORITY`, and `VITE_HOST_URL`. Keep environment-specific values coming from variable groups.
