Patch the deployment so the DU BFF App Service receives these environment variables from Azure DevOps variable group/app settings:

BFF_OIDC_ISSUER
BFF_OIDC_CLIENT_ID
BFF_OIDC_CLIENT_SECRET
BFF_OIDC_REDIRECT_URI
BFF_OIDC_SCOPES
BFF_OIDC_AUDIENCE
BFF_OIDC_POST_LOGIN_REDIRECT
SESSION_MAX_AGE

Add the required Bicep params and add them into the duApiAppSettings properties block in infra/templates/web-app.bicep. Also update the pipeline parameter mapping if deploy.steps.yml passes variable group values into the Bicep deployment. Do not hardcode environment-specific values or secrets.


I need to complete OneAccount/OIDC integration for the DU BFF app.

Context:
- The BFF code already reads OIDC values from environment variables in aiae-du-bff config.py using:
  BFF_OIDC_ISSUER
  BFF_OIDC_CLIENT_ID
  BFF_OIDC_CLIENT_SECRET
  BFF_OIDC_REDIRECT_URI
  BFF_OIDC_SCOPES
  BFF_OIDC_AUDIENCE
  BFF_OIDC_POST_LOGIN_REDIRECT
  SESSION_MAX_AGE

Goal:
- Pass these variables from Azure DevOps variable group into the DU BFF App Service application settings during deployment.
- Do not hardcode real values, secrets, URLs, or environment-specific data in the repo.
- Use pipeline/variable group values only.

Please do end-to-end repo changes:
1. Find where DU App/BFF App Service app settings are defined in Bicep.
2. Add Bicep parameters for the above BFF OIDC variables if missing.
3. Add those variables into the App Service app settings block for the BFF/DU API app.
4. Find the Azure DevOps deployment YAML where Bicep parameters are passed.
5. Map the parameters from existing Azure DevOps variable group variables.
6. Show me all changed files and explain exactly what variable names I need to add in the Azure DevOps variable group.
7. Do not modify frontend login code unless it is actually required.
