Act as a senior Azure Bicep and Azure Functions deployment engineer.

I am receiving this deployment error:

"Updating SQL Role Assignment Principal ID is not permitted. You may only
update the associated Role Definition."

The deployment error shows that the existing principal ID and the principal
ID being deployed are different.

I need you to investigate the complete repository and identify the exact root
cause. Do not make broad, destructive, or unrelated changes.

Tasks:

1. Search the entire repository for:
   - Microsoft.DocumentDB/databaseAccounts/sqlRoleAssignments
   - Microsoft.Authorization/roleAssignments
   - sqlRoleAssignments
   - principalId
   - roleDefinitionId
   - guid(
   - system-assigned and user-assigned managed identities
   - Cosmos DB role assignment modules
   - nested Bicep modules and deployment scripts
   - Function App-only deployment pipelines

2. Trace the complete deployment path:
   - pipeline YAML entry point
   - Bicep entry point
   - modules called by that entry point
   - the exact role-assignment resource causing the failure
   - which application's managed identity is passed as principalId
   - whether it belongs to the Function App, Web App, API, Orchestrator,
     deployment service principal, or another resource

3. Determine why the existing role-assignment resource name is being reused
   for a different principalId. Check whether the current resource name or
   GUID is generated without including principalId, for example:
      guid(cosmosAccount.id, roleDefinitionId)
   or whether it uses a fixed GUID.

4. Check whether the managed identity was recreated and therefore received a
   new principalId.

5. Propose the smallest idempotent Bicep correction. For every Cosmos DB SQL
   role assignment, the assignment name should be unique for the combination
   of:
   - Cosmos account or assignment scope
   - principalId
   - roleDefinitionId

   Prefer a deterministic expression similar to:
      guid(cosmosAccount.id, principalId, roleDefinitionId)

   Adapt it correctly to the repository structure and existing naming
   conventions.

6. Check separately for Microsoft.Authorization/roleAssignments. Their
   deterministic names should also include the assignment scope, principalId,
   and roleDefinitionId where appropriate.

7. Do not:
   - delete the Cosmos account
   - delete the Function App or Web App
   - delete all role assignments
   - replace managed identities
   - change environment names
   - modify Event Hub behaviour
   - modify application business logic
   - expose secrets
   - use newGuid() for repeatable infrastructure deployments
   - make unrelated formatting changes

8. Check why the pipeline described as "Function App-only deployment" still
   attempts to deploy Cosmos SQL role assignments. Identify whether it invokes
   the full app.bicep, a shared role module, or another infrastructure
   deployment.

9. Determine whether adding the temporary HTTP trigger requires:
   A. only a Function App package/ZIP deployment, or
   B. an infrastructure deployment.
   If no new resource, identity permission, or app setting is required,
   recommend the existing code-only deployment path.

10. Before editing anything, report:
    - failing file and line
    - failing Bicep symbolic resource name
    - generated role-assignment name/GUID logic
    - existing principal source
    - new principal source
    - root cause
    - proposed files to change
    - whether an obsolete assignment requires one-time cleanup

11. Then make only the minimum necessary changes.

12. After making the changes:
    - show the exact diff
    - run or recommend bicep build
    - run or recommend bicep lint
    - run a what-if deployment
    - confirm no resource deletion or replacement is planned
    - provide the exact deployment command already consistent with this repo

13. If an old Cosmos SQL role assignment must be removed, do not remove it
    automatically. Provide a read-only command to list assignments first.
    Then provide the exact delete command separately and clearly state which
    assignment GUID and old principal it would remove.

14. Also inspect the deployment error for any second independent failure.
    Do not assume every failed deployment is caused by the same role
    assignment. Report each failure separately.

Start with investigation and findings. Do not edit files until the root cause
and proposed minimal diff are shown.





I do not want to introduce a new Bicep file or module.

Please undo and remove:

infra/templates/cosmos-sql-role-assignment.bicep

Do not delete or change any Azure resources. Only revise the local code.

The deployment failure is:

"Updating SQL Role Assignment Principal ID is not permitted."

Implement the smallest permanent fix using only the existing Bicep templates.

Tasks:

1. Locate every existing resource of type:

   Microsoft.DocumentDB/databaseAccounts/sqlRoleAssignments

   especially in:
   - infra/templates/app.bicep
   - infra/templates/web-app.bicep
   - infra/templates/cosmos-nosql.bicep
   - any other existing template

2. Confirm the exact failing resources and report their:
   - file
   - line
   - symbolic name
   - current role-assignment name expression
   - current principalId source
   - role definition
   - scope

3. The root cause is expected to be that the role-assignment name is based
   only on application/resource names while principalId comes from a managed
   identity. When the managed identity principal changes, Azure tries to
   update an existing Cosmos SQL role assignment, but principalId is
   immutable.

4. Do not create any new file or folder.

5. Replace the newly created module calls with direct
   Microsoft.DocumentDB/databaseAccounts/sqlRoleAssignments resources inside
   the existing app.bicep and web-app.bicep files.

6. Because the assignment name must include principalId, determine whether
   the principal ID can be passed into each existing template as a deployment
   parameter known at the start of deployment.

7. Prefer this pattern:

   param cosmosIdentityPrincipalId string

   name: guid(
     databaseAccount.id,
     cosmosIdentityPrincipalId,
     roleDefinitionGuid
   )

   properties: {
     roleDefinitionId:
       '${databaseAccount.id}/sqlRoleDefinitions/${roleDefinitionGuid}'
     principalId: cosmosIdentityPrincipalId
     scope: databaseAccount.id
   }

8. Trace where the managed identity is created or referenced and update the
   existing parent template or pipeline parameter mappings to pass the
   correct principal/object ID.

9. Do not use:
   - client ID instead of principal ID
   - managed identity resource ID instead of principal ID
   - identity name as a substitute for principal ID
   - newGuid()
   - hard-coded environment-specific principal IDs in shared Bicep
   - automatic role-assignment deletion

10. If the principalId cannot be passed as a start-of-deployment parameter,
    stop and explain clearly why the no-new-file implementation is not
    technically possible with the current deployment structure. Do not
    recreate the separate module unless I approve it.

11. Check every environment parameter path:
    - DV
    - QA
    - UAT/UT
    - PP
    - PR

    Confirm that each environment will receive the correct principal ID.

12. Preserve the existing Cosmos NoSQL account, databases, containers,
    identities, application settings, Event Hub integration, and HTTP trigger.

13. Show the exact final diff before applying it.

14. Validate using:
    - az bicep build
    - az bicep lint
    - deployment what-if

15. The what-if must show:
    - no Cosmos DB replacement
    - no Function App replacement
    - no Web App replacement
    - no managed identity replacement
    - no databases or containers deleted
    - only intended Cosmos SQL role assignments created

Do not make unrelated changes.












I have undone the latest no-new-file/two-pass pipeline implementation.

Please inspect the current working tree only. Do not make any edits.

Confirm whether the repository is now back to the single reusable
cosmos-sql-role-assignment.bicep module approach.

Report:

1. Current changed and untracked files.
2. Whether pipelines/deploy.steps.yml still contains any AzureCLI identity
   lookup or "Finalize Function/Web App Cosmos SQL role assignments" steps.
3. Whether app.bicep and web-app.bicep use module calls to
   cosmos-sql-role-assignment.bicep.
4. For each module call:
   - calling file
   - symbolic module name
   - principalId source
   - Cosmos role
   - Cosmos account
5. Confirm that the module assignment name uses:
   guid(databaseAccount.id, principalId, roleDefinitionGuid)
6. Confirm that no Cosmos account, database, container, Function App,
   Web App, or managed identity is being renamed or replaced.
7. Check for duplicate direct sqlRoleAssignments left alongside the module
   calls.
8. Check cosmos-nosql.bicep and confirm whether its user-group assignment uses
   a stable GUID containing account, group principal, and role.
9. Run Bicep build/lint if available.
10. Provide the exact commands for a Dev what-if deployment.

Do not change any files.
