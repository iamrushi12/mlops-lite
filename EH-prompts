Act as a senior Azure Python developer. Inspect this repository and implement the Event Hub publishing changes required for the current ticket.

## Ticket objective

The existing Document Understanding application must publish data to Azure Event Hub from two points in the current processing flow:

1. After the pre-decision/confidence-processing data is available.
2. After the post-HITL process is completed and the final corrected data is available.

The downstream Data Engineering team will handle Event Hub ingestion, Data Lake storage, and identifying/processing the events.

Our responsibility is only to publish the correct existing application data to Event Hub from the correct code locations.

## Important scope clarification

Do not invent or enforce a new custom event-envelope structure.

Do not add fields such as:

- event_type
- event_version
- source
- environment
- correlation_id
- custom timestamps

unless those fields already exist in the current payload, are already required by an existing schema, or are clearly required by the existing implementation.

Reuse the payloads and field names already available in the application.

Do not rename existing business fields.

Do not reintroduce the old `pipeline_id` field if the repository already uses `pipeline_definition_id`.

The published Event Hub messages must contain the existing data available at each publishing point, including pipeline IDs and other identifiers already present in the application.

## First inspect the repository

Before changing anything, inspect and identify:

1. The service or function responsible for confidence/pre-decision processing.
2. The exact code location where the pre-decision payload is fully available.
3. The service or API responsible for HITL completion.
4. The exact code location where the final post-HITL payload is fully available.
5. Existing request/response models, Pydantic models, dictionaries, schemas, Cosmos records, or serialization helpers that already represent these payloads.
6. The existing Azure DevOps variable-group and deployment configuration pattern.
7. Whether the services use synchronous or asynchronous Python.
8. Whether `azure-eventhub` is already included in the deployed service dependencies.

Do not make assumptions based only on filenames. Trace the actual application flow.

## Event Hub configuration

The Event Hub connection string is stored securely in an Azure DevOps Library variable group.

Use this application-setting/environment-variable name unless the repository already has an agreed equivalent:

`EVENT_HUB_CONNECTION_STRING`

Do not hardcode any real connection string, key, namespace, or secret in source control.

Also support an optional Event Hub name setting:

`EVENT_HUB_NAME`

Use `EVENT_HUB_NAME` only when the connection string does not already contain `EntityPath`.

The implementation must support both cases:

1. Event Hub connection string contains `EntityPath`.
2. Namespace-level connection string is provided and the Event Hub entity name comes from `EVENT_HUB_NAME`.

Never log the connection string.

## Azure DevOps and infrastructure mapping

Inspect the repository’s current variable-group and application-setting mapping.

Add the Event Hub settings only where required, using the repository’s existing conventions.

Likely locations may include:

- Azure DevOps deployment YAML
- service deployment templates
- Bicep application settings
- variable files
- configuration/settings classes

Map the Azure DevOps Library variable-group secret into the relevant Function App or Web App settings.

Use the actual repository syntax rather than blindly applying this example:

`EVENT_HUB_CONNECTION_STRING: $(EVENT_HUB_CONNECTION_STRING)`

If `EVENT_HUB_NAME` is needed, map it as a non-secret configuration value.

Only add these settings to the application services that actually publish the events.

Assume the Event Hub resource already exists. Do not create a new Event Hub namespace or Event Hub unless the current infrastructure explicitly requires it.

## Event Hub publisher implementation

Create one reusable Event Hub publisher helper/service instead of duplicating connection and send logic in multiple business files.

The publisher must:

1. Read configuration through the repository’s existing settings mechanism.
2. Create an `EventHubProducerClient`.
3. Accept an existing Python dictionary, model, or payload object.
4. Convert the existing payload into valid JSON.
5. Use UTF-8 encoding.
6. Create an Event Data batch.
7. Add the serialized payload to the batch.
8. Send the batch to Event Hub.
9. Close the producer safely.
10. Log success using safe identifiers only.
11. Log failures without exposing secrets or complete sensitive payload contents.
12. Follow the repository’s existing synchronous or asynchronous style.

Do not redesign the application architecture unnecessarily.

If the project uses dependency injection or application lifecycle services, integrate the producer consistently.

If the current implementation is synchronous, do not introduce async Event Hub code without a strong repository-specific reason.

## Payload rules

Publish the existing application payload available at each stage.

Do not create a large wrapper object around it.

Do not replace the real payload with a reduced payload containing only IDs unless that is already the agreed payload in the code or ticket.

Do not send raw non-serializable internal objects directly.

Use existing serializers or models wherever possible.

Ensure the payload is JSON serializable:

- Convert datetime values to ISO-8601 strings.
- Convert UUIDs or custom IDs to strings.
- Convert enum values correctly.
- Handle optional values safely.
- Avoid double JSON encoding.
- Do not convert an already structured object into an escaped JSON string inside another JSON object.

If the existing application already exposes methods such as:

- `model_dump()`
- `dict()`
- `json()`
- existing payload builders
- existing response serializers

reuse them appropriately.

## Pre-decision publishing

Find the exact point where the confidence/pre-decision data is complete and before the application continues to the confidence-decision or HITL-routing stage.

At this point, publish the existing pre-decision payload to Event Hub.

The payload should include whatever is already available in the application, such as:

- submission ID
- pipeline definition ID
- pipeline execution ID
- document ID
- extraction data
- confidence data
- status
- document metadata
- other existing identifiers or processing data

Do not invent missing values.

Do not modify or remove the existing decision-engine or routing call.

The Event Hub publishing code should be inserted into the existing flow with minimal disruption.

## Post-HITL publishing

Find the exact point where HITL has been completed and the final corrected or approved payload is available.

At this point, publish the existing post-HITL/final payload to Event Hub.

Use the real final payload available in the application.

This may include:

- submission ID
- pipeline definition ID
- pipeline execution ID
- document ID
- corrected extraction data
- final status
- approval or correction result
- other existing final metadata

Do not invent missing fields.

Do not change the existing HITL, export, or final-processing behaviour.

## Distinguishing pre-decision and post-HITL data

Do not automatically add a new `event_type` field.

First inspect whether the two messages are already distinguishable through:

- different Event Hubs
- different producer services
- different payload structures
- existing status fields
- existing pipeline definition IDs
- existing process-stage fields
- separate downstream routes

Only add a small stage indicator such as `event_type` or `processing_stage` if:

1. there is currently no way to distinguish the two messages, and
2. adding it does not break the existing payload contract.

If such a field is needed, keep the values minimal and clear:

- `pre_decision`
- `post_hitl`

Before adding it, explicitly mention this as an assumption in the final summary.

## Error handling

The Event Hub integration must not corrupt the current document-processing workflow.

Follow the repository’s existing exception-handling pattern.

At minimum:

- Log which publishing stage failed.
- Log safe IDs where available.
- Preserve useful exception details.
- Never log the connection string.
- Do not log the complete payload if it may contain document-sensitive data.
- Do not report success when publishing failed.
- Close the producer safely.

Inspect the current ticket flow and repository behaviour to decide whether Event Hub failure should:

- stop the current operation, or
- be logged while the main business flow continues.

Do not choose this arbitrarily.

Use the existing ticket requirements, current error-handling pattern, and surrounding code behaviour.

If this cannot be confirmed from the repository, keep the behaviour minimally invasive and clearly state the assumption in the final summary.

Do not add infinite retries.

If retry handling is needed and the repository has no existing retry mechanism, use only a small bounded retry for transient Event Hub/network failures.

## Dependency changes

Check whether the correct deployed Python service already contains the Azure Event Hubs SDK.

If not, add:

`azure-eventhub`

to the correct dependency file used during deployment.

Do not upgrade unrelated packages.

## Tests

Add or update unit tests using mocks only.

Do not connect to a real Azure Event Hub.

Test at least:

1. Event Hub publisher serializes and sends a valid existing payload.
2. Pre-decision publishing is called from the correct workflow point.
3. Post-HITL publishing is called from the correct workflow point.
4. Existing payload field names are preserved.
5. Datetime, UUID, enum, and optional values serialize correctly.
6. A connection string containing `EntityPath` works without `EVENT_HUB_NAME`.
7. A namespace-level connection string uses `EVENT_HUB_NAME`.
8. Missing configuration produces a clear controlled error.
9. Publishing failures follow the selected error-handling behaviour.
10. Secrets and full sensitive payloads are not logged.

Use the repository’s existing testing framework and conventions.

Mock the Azure Event Hub producer and send operation.

## Restrictions

Do not:

- create a separate test Function App
- create a standalone proof-of-concept application
- create a Cloud Shell test script
- create a dry-run implementation
- create a new Event Hub resource
- create a Data Lake integration
- implement Power BI logic
- replace Cosmos DB
- modify unrelated business logic
- hardcode connection strings
- invent a new payload schema
- duplicate the publisher code
- send placeholder or fake business values

Implement the change directly in the existing application code.

## Required final response

After making the changes, provide:

1. The exact files changed.
2. A concise explanation of each change.
3. The exact function and code location used for pre-decision publishing.
4. The exact function and code location used for post-HITL publishing.
5. The existing payload/model used for each event.
6. Whether any new stage-identification field was added and why.
7. The Azure DevOps Library variables required:
   - `EVENT_HUB_CONNECTION_STRING`
   - `EVENT_HUB_NAME`, only if required
8. The exact deployment mapping changes.
9. The test files added or updated.
10. The command to run the relevant tests.
11. Any assumptions or unresolved points discovered from the repository.

Do not only explain what should be done. Inspect the repository and implement the actual code changes.






Review the current Event Hub implementation without making changes first.

Verify specifically:

1. Whether PipelineOutputService.update_content is called only after final HITL submission or can also be called for intermediate saves.
2. Whether every post-HITL event always contains submission ID, document ID, and the correct pipeline_definition_id/pipeline execution identifier.
3. Whether the pre-decision and post-HITL payloads could exceed Event Hub message limits or contain sensitive document information.
4. Whether EVENT_HUB_NAME is actually required based on the connection-string format used by this repository.
5. Whether all modified dependency files are genuinely used by the Function App and DU API deployment builds.
6. Whether the synchronous Event Hub publisher is being called directly from an async DU API method and could block the event loop.
7. Whether Event Hub send failures are intentionally logged and ignored, or whether the ticket expects the business operation to fail.

Do not modify files yet. Return exact file names, functions, line numbers, findings, and recommended corrections.





Review the existing Azure Functions Python project and add one isolated, temporary HTTP-triggered function for testing outbound connectivity from this Function App to Azure Event Hub.

Important constraints:

1. Do not modify, refactor, rename, or remove any existing functions, triggers, services, routes, business logic, configuration, or tests.
2. Do not change the existing application flow.
3. Add only the minimum files and code required for the temporary connectivity test.
4. First inspect the repository and identify whether it uses:
   - Azure Functions Python v1 programming model with function.json, or
   - Azure Functions Python v2 programming model with decorators and FunctionApp/Blueprint.
5. Follow the existing project structure and coding conventions.
6. Do not create a separate Function App. Add one additional HTTP trigger inside the existing Function App.

Temporary function requirements:

- Function/route name: eventhub_connectivity_test
- HTTP methods: POST only
- Authorization level: FUNCTION, not ANONYMOUS
- Purpose: accept a JSON payload from an HTTP request and publish that payload as one event to Azure Event Hub.
- Return HTTP 200 only after Event Hub confirms the send operation completed.
- Return HTTP 400 for missing, empty, or invalid JSON.
- Return HTTP 500 for Event Hub connection, authentication, timeout, or publishing failures.
- Log useful status information, but never log credentials, connection strings, SAS keys, authorization headers, or sensitive payload fields.

Configuration:

- Read the Event Hub connection string only from the Function App environment variable:

  EVENT_HUB_CONNECTION_STRING

- Do not hardcode the connection string anywhere.
- I will map EVENT_HUB_CONNECTION_STRING from the Azure DevOps variable group into the Function App app settings.
- Support both types of connection strings:

  1. Event Hub-specific connection string containing:
     EntityPath=<event-hub-name>

     In this case, create the producer without passing eventhub_name separately.

  2. Namespace-level connection string without EntityPath.

     In this case, read the actual Event Hub entity name from:

     EVENT_HUB_NAME

     and pass it through eventhub_name.

- Never use the Event Hubs namespace name as eventhub_name.
- Validate the required configuration when the request is received and return a clear configuration error without exposing secret values.

Event Hub implementation:

- Use:
  azure-eventhub

- Import:
  EventHubProducerClient
  EventData
  TransportType

- Configure the producer with:

  transport_type=TransportType.AmqpOverWebsocket

- Use a context manager or finally block so the producer is always closed.
- Serialize the received request JSON using json.dumps().
- Send exactly one EventData message for each HTTP request.
- Preserve the complete incoming JSON object without introducing a custom business wrapper.
- Do not add an Event Hub trigger or output binding. Use EventHubProducerClient directly because this function is only for controlled connectivity testing.
- Add a reasonable connection timeout/retry configuration only if it is supported by the installed Azure Event Hub SDK. Do not invent unsupported parameters.

Response format:

Successful response:

{
  "status": "success",
  "message": "Event published successfully",
  "target": "configured Event Hub entity"
}

Invalid request response:

{
  "status": "failed",
  "message": "Request body must contain valid JSON"
}

Publishing failure response:

{
  "status": "failed",
  "message": "Failed to publish event",
  "errorType": "<safe exception class name>"
}

Do not expose the full exception if it could contain credentials or the complete connection string.

Repository updates:

1. Add azure-eventhub to the existing requirements.txt only if it is not already present.
2. Do not upgrade or change unrelated dependencies.
3. Do not edit host.json unless the new trigger genuinely requires it.
4. Do not modify infrastructure or pipeline files unless EVENT_HUB_CONNECTION_STRING and EVENT_HUB_NAME must be mapped into the Function App app settings.
5. If app-setting mappings are required, show the exact proposed changes first and keep them limited to:
   - EVENT_HUB_CONNECTION_STRING
   - EVENT_HUB_NAME
6. Do not place secret values in variables.yml, environment-specific YAML files, source code, local.settings.json committed to Git, or logs.
7. Add a clear comment marking the trigger as TEMPORARY and safe to remove after connectivity validation.

Before editing:
- Briefly tell me which Azure Functions programming model the repository uses.
- Tell me which files you intend to modify or create.

Then make the changes and provide:
- The complete code for the temporary HTTP trigger.
- A concise list of modified files.
- The expected HTTP endpoint.
- One curl example using a sample JSON payload.
- The environment-variable names that must exist after deployment.
- Instructions for removing the temporary trigger after testing.



connectivity:-

Please update the repository to make the temporary Function App HTTP trigger `eventhub_connectivity_test` deploy and run entirely through code and pipelines, without any manual Azure Portal or terminal changes.

Make only the minimum required changes. Do not modify unrelated web apps, poller services, orchestrator services, Cosmos DB role assignments, storage accounts, existing triggers, or business logic.

Requirements:

1. Function dependency
- In `src/functions/requirements.txt`, ensure the Azure Event Hubs Python SDK is included:
  `azure-eventhub>=5.11,<6`
- Do not remove or downgrade existing dependencies.

2. Function package pipeline
- Find the pipeline template that builds `doc-understanding-build.zip`, likely `pipelines/build/function-package.yml`.
- Before archiving the Function App, install dependencies from:
  `src/functions/requirements.txt`
- Install them into:
  `src/functions/.python_packages/lib/site-packages`
- Use `python -m pip`.
- Ensure the archive is created from the contents of `src/functions` with `includeRootFolder: false`.
- The ZIP root must contain:
  `function_app.py`
  `host.json`
  `requirements.txt`
  `.python_packages`
  `services`
- Do not create a ZIP containing an extra `src/functions` parent directory.
- Preserve the existing artifact name and publish location expected by `deploy.steps.yml`.

3. Function App infrastructure parameters
- Find the Bicep template used by the task named:
  `Setup App Resources FunctionApp, Keyvault, StorageAccounts and AppInsight`
- Add these parameters if they do not already exist:

  `@secure()`
  `param eventHubConnectionString string`

  `param eventHubName string`

- Add these values to the existing Function App app settings:

  `EVENT_HUB_CONNECTION_STRING = eventHubConnectionString`
  `EVENT_HUB_NAME = eventHubName`

- Preserve all existing app settings. Do not replace the entire settings collection.
- Do not hard-code either value.
- Do not expose the connection string in outputs, logs, or comments.

4. Deployment pipeline mapping
- In `pipelines/deploy.steps.yml`, update only the existing Function App infrastructure deployment task.
- Pass the Azure DevOps variable-group values into the Bicep template using:

  `-eventHubConnectionString "$(EVENT_HUB_CONNECTION_STRING)"`
  `-eventHubName "$(EVENT_HUB_NAME)"`

- Keep these tasks enabled:
  - Function App infrastructure/setup deployment
  - Function App code deployment
- Do not enable unrelated Web App, Poller, UI, Orchestrator, Cosmos, or role-assignment tasks.

5. Temporary HTTP trigger
- Preserve the existing `eventhub_connectivity_test` HTTP POST trigger in `src/functions/function_app.py`.
- It must read:
  `EVENT_HUB_CONNECTION_STRING`
  `EVENT_HUB_NAME`
- If the connection string already contains `EntityPath`, use that entity and do not pass a duplicate `eventhub_name`.
- Otherwise use `EVENT_HUB_NAME`.
- Continue using:
  `TransportType.AmqpOverWebsocket`
- Keep `auth_level=func.AuthLevel.FUNCTION`.
- Do not log credentials or the full connection string.
- Return a safe JSON success or failure response.

6. Validation
After making the changes, show me:
- the exact files changed;
- a concise diff summary;
- the final package path used by the deploy task;
- confirmation that no unrelated files were modified.

Do not run deployments, create new Azure resources, add new role-assignment templates, or make broad refactoring changes. Only update the repository files needed for this Function App connectivity test.



duapi fix:-

Fix only the DU API artifact packaging error:

"Invalid requirement: '../common'. File '../common' does not exist."

Do not modify Event Hub publisher logic, Function App code, Bicep files,
frontend source code, Cosmos resources, or application behavior.

In pipelines/build/function-package.yaml, update only the BuildDuApiWebApp job:

1. Create a clean staging folder under:
   $(Build.ArtifactStagingDirectory)/du-api-package

2. Copy all files from:
   $(Build.SourcesDirectory)/src/du_api_webapp
   into the staging folder.

3. Copy all files from:
   $(Build.SourcesDirectory)/src/common
   into:
   $(Build.ArtifactStagingDirectory)/du-api-package/common

4. In only the staged requirements.txt, replace the exact line:
   ../common
   with:
   ./common

5. Do not modify the source requirements.txt file.

6. Validate that these files exist before archiving:
   - du-api-package/requirements.txt
   - du-api-package/common/pyproject.toml
   - du-api-package/common/common/eventhub/publisher.py

7. Archive the staging folder with includeRootFolder=false as:
   $(Build.ArtifactStagingDirectory)/du-api-webapp.zip

8. Keep the existing artifact name du-api-webapp.

9. Do not add frontend extraction, npm install, npm build, or repackaging
   steps.

Show the exact YAML diff before applying it.



The "Prepare staged DU API package" step is failing with exit code 1 because one of the validation paths is incorrect.

Please review the staging script in pipelines/build/function-package.yaml and fix only the validation checks. Verify that the paths match the actual structure created after copying src/common and src/axa_packages into $(Build.ArtifactStagingDirectory)/du-api-package.

Do not change the packaging logic, deployment steps, or requirements rewriting. Only correct the failing validation path and add a couple of echo statements to print the staged folder structure for debugging.




final fix 

Fix the Azure DevOps cyclical variable warnings for EVENT_HUB_CONNECTION_STRING and EVENT_HUB_NAME.

Find and remove any self-referencing definitions such as:
EVENT_HUB_CONNECTION_STRING: $(EVENT_HUB_CONNECTION_STRING)
EVENT_HUB_NAME: $(EVENT_HUB_NAME)

Use the values directly from the existing variable group in deploy.steps.yml, or map them to differently named pipeline variables. Do not change Bicep parameters, Event Hub code, or other variables.




query :


traces
| where timestamp > ago(7d)
| where message contains "CombineExtractionOutputs"
| order by timestamp des




jsin correction:-


Please make a minimal and safe fix for the JSONDecodeError occurring in the CombineExtractionOutputs flow.

Current error:
requests.exceptions.JSONDecodeError:
Expecting value: line 1 column 1 (char 0)

The traceback points to:
- function_app.py, inside combine_extraction_outputs_http
- services/post_processing/combine_json.py
- upload_json_output()
- the failing statement is approximately: result = response.json()

Requirements:

1. First inspect the existing implementation of:
   - combine_extraction_outputs_http
   - upload_json_output()
   - the HTTP request that produces the `response`
   - all current callers and tests

2. Do not change the business flow, Event Hub payload, function routes, infrastructure, pipeline YAML, Bicep files, application settings, authentication logic, storage paths, or unrelated code.

3. Make only the smallest necessary change inside the response-handling logic of `upload_json_output()`.

4. Before parsing the response, log the following safely:
   - HTTP status code
   - Content-Type header
   - whether the body is empty
   - a truncated response body, maximum 1000 characters

5. Do not log secrets, authorization headers, tokens, connection strings, or full sensitive payloads.

6. Call `response.raise_for_status()` before treating the request as successful.

7. Parse JSON only when:
   - the response body is not empty, and
   - the response Content-Type indicates JSON

8. Correctly handle successful empty responses such as HTTP 204 without raising JSONDecodeError.

9. Preserve the current return type and expected behaviour of `upload_json_output()`.
   Inspect how its return value is used before deciding what should be returned for an empty successful response.
   Do not blindly return `{}` if callers expect a filename, blob name, URL, string, or another specific type.

10. For a successful non-JSON response:
    - log a clear warning
    - preserve compatibility with the current caller
    - do not silently hide an invalid downstream response if the caller requires JSON data

11. For unsuccessful HTTP responses:
    - allow `raise_for_status()` to raise the appropriate HTTP exception
    - include useful status/body information in logs
    - do not convert every error into a generic JSONDecodeError

12. Add or update focused unit tests covering:
    - 200 response with valid JSON
    - 201 response with valid JSON, if relevant
    - 204 response with an empty body
    - 200 response with an empty body
    - 200 response with plain text or HTML
    - 401/403/404/500 response
    - malformed JSON with application/json Content-Type

13. Do not move the Event Hub publishing call or change whether it happens before or after `upload_json_output()`. Only identify and report its current ordering.

14. After making the change, provide:
    - files changed
    - exact reason for each change
    - the final code diff
    - tests added or updated
    - test results
    - confirmation that no deployment, infrastructure, Event Hub, authentication, or unrelated files were modified

Important:
Do not refactor the whole function or introduce new abstractions unless absolutely required. Keep the patch small, backward-compatible, and limited to resolving and diagnosing the response parsing failure.
