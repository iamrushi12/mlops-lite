Update the Event Hub authentication implementation to use our System-Assigned Managed Identity + federated workload identity architecture. Remove all connection-string and ClientSecretCredential authentication and completely remove EVENT_HUB_CLIENT_SECRET. Do not add Function App/HITL Object IDs or managed-identity Client IDs to runtime variables because both Function Apps use System-Assigned Managed Identity, which Azure should discover automatically with ManagedIdentityCredential(). Use the system-assigned MI to obtain an api://AzureADTokenExchange/.default assertion, then use ClientAssertionCredential with only the federated App Registration Application/Client ID and the UK target Tenant ID. Use that credential with EventHubProducerClient. Keep only EVENT_HUB_APP_CLIENT_ID, EVENT_HUB_TENANT_ID, EVENT_HUB_FULLY_QUALIFIED_NAMESPACE, and EVENT_HUB_NAME in the deployment/variable configuration. Update publisher.py, Bicep templates, deploy.steps.yml, environment-variable mapping and any related Python configuration consistently. Do not change the existing pre-decision or post-HITL payload schemas, filtering, queue logic, or publishing behaviour; only replace the authentication mechanism so both flows continue sending the same payloads to Event Hub.
